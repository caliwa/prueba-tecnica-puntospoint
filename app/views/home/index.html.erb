<style>
  body {
    background-color: #4a148c;
    color: white;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    text-align: center;
  }

  .container {
    padding: 2rem;
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
  }

  .btn-docs {
    display: inline-block;
    background-color: #ffffff;
    color: #4a148c;
    padding: 15px 30px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: bold;
    font-size: 1rem;
    border: 2px solid #ffffff;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    display: none;
  }

  /* Efecto al pasar el mouse por encima */
  .btn-docs:hover {
    background-color: #f3e5f5;
    color: #4a148c;
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
  }

  .password-container {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  input[type="password"] {
    padding: 10px;
    margin: 10px;
    border: 2px solid #fff;
    border-radius: 5px;
    font-size: 1rem;
    background-color: transparent;
    color: white;
  }

  button {
    background-color: #ffffff;
    color: #4a148c;
    padding: 10px 20px;
    border-radius: 50px;
    font-weight: bold;
    font-size: 1rem;
    border: 2px solid #ffffff;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  button:hover {
    background-color: #f3e5f5;
    color: #4a148c;
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
  }
  .error-message {
    color: #ff8a80;
    margin-top: 1rem;
    font-weight: bold;
  }
</style>


<div class="container">
  <h1>Puntospoint</h1>
  <p>Por favor, ingresa la clave para acceder a la documentación de la API:</p>

  <%# Muestra el mensaje de error si existe %>
  <% if @error_message %>
    <p class="error-message"><%= @error_message %></p>
  <% end %>

  <div class="password-container">
    <input type="password" id="password" placeholder="Ingrese la clave" required>
    <button id="submit-btn" onclick="validatePassword()">Acceder</button>
  </div>
</div>

<script>
  function validatePassword() {
    const enteredPassword = document.getElementById('password').value;

    if (enteredPassword) {
      window.location.href = `/?password=${encodeURIComponent(enteredPassword)}`;
    } else {
      alert("Por favor, ingresa una clave.");
    }
  }

  document.getElementById('password').addEventListener('keypress', function(event) {
    if (event.key === 'Enter') {
      event.preventDefault();
      validatePassword();
    }
  });
</script>